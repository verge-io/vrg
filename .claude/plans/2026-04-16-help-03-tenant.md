# Help Text Improvement: `vrg tenant`

**Date:** 2026-04-16
**Status:** Draft
**Scope:** Tenant command group and subcommands (tenant, node, storage, net, snapshot, stats, share, recipe, recipe-instance)
**Depends on:** help-00-root (must be done first to enable `rich_markup_mode="markdown"` on the main app)

---

## Conventions (apply to all help text in this plan)

### Where help text lives

- **Group apps** (`typer.Typer()`): Help goes in the `help=` parameter. The callback docstring is ignored when `help=` is set — keep callback docstrings to one line (e.g., `"""Manage virtual machines."""`).
- **Leaf commands** (`@app.command()`): Help comes from the function docstring. This is the only place Typer reads it.
- Every `typer.Typer()` must have `rich_markup_mode="markdown"` so markdown renders in terminal output.

### Section order for group `help=` strings

1. **Description** (1-3 sentences): What this resource is in VergeOS and why it matters. Not just "Manage X."
2. **Agent guidance** (1-2 sentences): Which flags produce machine-readable output, what fields are useful to `--query`, any gotchas.
3. **Examples** (3-6 invocations): Real commands showing common workflows. Use `---` markdown separator before and after.
4. **Notes** (optional): Behavioral quirks, ordering of operations, required preconditions.

### Section order for leaf command docstrings

1. **One-line summary** (what the command does).
2. **1-3 examples** showing invocation with typical arguments.
3. **Behavioral note** (optional): e.g., "This is a destructive operation" or "Requires the VM to be stopped."

### Quality bar

- Examples must use realistic resource names (e.g., `web-01`, `internal-net`, `tier-2`), not placeholders like `<name>`.
- Every group must mention `-o json` for structured output somewhere.
- Exit code 7 (multiple matches) should be mentioned where name resolution applies.
- Match the voice of showboat/tracecc: plain English, no filler, designed for agents to parse.

### Commit discipline

Make atomic commits as you go — one commit per logical unit of work (e.g., one
subgroup file complete, or all leaf docstrings in a file). Do not batch all
changes into a single commit at the end. Run tests before each commit.

## Overview

Tenants are one of VergeOS's most distinctive features — they're complete nested VergeOS instances, not just resource partitions. Currently "Manage tenants." tells an agent nothing.

## What VergeOS tenants are (source from docs)

A tenant is a fully isolated Virtual Data Center (VDC) — a complete nested VergeOS instance with its own virtual nodes, networking, storage quotas, user management, and web UI. Tenants can host their own sub-tenants (nested multi-tenancy). Up to 4,000 tenants per system. Each tenant has allocated CPU, RAM, and storage resources from the host.

## Source files to modify

| File | Current help |
|------|-------------|
| `commands/tenant.py` | "Manage tenants." |
| `commands/tenant_node.py` | "Manage tenant nodes." |
| `commands/tenant_storage.py` | "Manage tenant storage." |
| `commands/tenant_net.py` | "Manage tenant networks." |
| `commands/tenant_snapshot.py` | "Manage tenant snapshots." |
| `commands/tenant_stats.py` | "Manage tenant statistics." |
| `commands/tenant_share.py` | "Manage tenant shared objects." |
| `commands/tenant_recipe.py` | "Manage tenant recipes." |
| `commands/tenant_recipe_instance.py` | "Manage tenant recipe instances." |

## Examples to include

```
# List all tenants
vrg tenant list

# Get tenant details
vrg -o json tenant get acme-corp

# Create a tenant
vrg tenant create --name acme-corp --cores 8 --ram 16384

# Power operations
vrg tenant start acme-corp
vrg tenant stop acme-corp

# View tenant resource usage
vrg tenant stats get acme-corp

# Manage tenant nodes
vrg tenant node list acme-corp

# Manage tenant networking
vrg tenant net list acme-corp

# Snapshot a tenant
vrg tenant snapshot create acme-corp --name before-migration
vrg tenant snapshot list acme-corp
```

### Behavioral notes

```
Notes:
  Tenants are complete nested VergeOS instances, not containers or
  resource partitions. Each tenant runs its own virtual nodes, has
  independent networking, and can be managed through its own UI.

  Tenant operations affect the entire nested environment. Stopping a
  tenant stops all VMs and services running inside it.

  Tenants are identified by name or numeric key.
```

## Documentation references

- Wiki tenant concept: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/tenant.md`
- Wiki tenant provisioning: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/tenant-provisioning.md`
- Wiki tenant resources: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/tenant-resource-management.md`
- Wiki tenant networking: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/tenant-network-isolation.md`
- Wiki tenant storage: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/tenant-storage-quotas.md`
- Public docs tenants: `/Volumes/HOME/projects/docs/docs/product-guide/tenants/`
- Wiki sites & tenants overview: `/Volumes/HOME/verge-vault/VergeOS/wiki/overviews/sites-and-tenants.md`

## Checklist

- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_net.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_node.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_share.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_snapshot.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_stats.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `tenant_storage.py`
- [x] Add examples to leaf command docstrings (Typer reads these as help text for `@app.command()` functions)
- [x] Test all help output renders correctly
- [x] Commit changes
