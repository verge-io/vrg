# Help Text Improvement: `vrg nas`

**Date:** 2026-04-16
**Status:** Draft
**Scope:** NAS command group and subcommands (nas, service, volume, volume-snapshot, cifs, nfs, user, sync, files)
**Depends on:** help-00-root (must be done first to enable `rich_markup_mode="markdown"` on the main app)

---

## Conventions (apply to all help text in this plan)

### Where help text lives

- **Group apps** (`typer.Typer()`): Help goes in the `help=` parameter. The callback docstring is ignored when `help=` is set — keep callback docstrings to one line (e.g., `"""Manage virtual machines."""`).
- **Leaf commands** (`@app.command()`): Help comes from the function docstring. This is the only place Typer reads it.
- Every `typer.Typer()` must have `rich_markup_mode="markdown"` so markdown renders in terminal output.

### Section order for group `help=` strings

1. **Description** (1-3 sentences): What this resource is in VergeOS and why it matters. Not just "Manage X."
2. **Agent guidance** (1-2 sentences): Which flags produce machine-readable output, what fields are useful to `--query`, any gotchas.
3. **Examples** (3-6 invocations): Real commands showing common workflows. Use `---` markdown separator before and after.
4. **Notes** (optional): Behavioral quirks, ordering of operations, required preconditions.

### Section order for leaf command docstrings

1. **One-line summary** (what the command does).
2. **1-3 examples** showing invocation with typical arguments.
3. **Behavioral note** (optional): e.g., "This is a destructive operation" or "Requires the VM to be stopped."

### Quality bar

- Examples must use realistic resource names (e.g., `web-01`, `internal-net`, `tier-2`), not placeholders like `<name>`.
- Every group must mention `-o json` for structured output somewhere.
- Exit code 7 (multiple matches) should be mentioned where name resolution applies.
- Match the voice of showboat/tracecc: plain English, no filler, designed for agents to parse.

### Commit discipline

Make atomic commits as you go — one commit per logical unit of work (e.g., one
subgroup file complete, or all leaf docstrings in a file). Do not batch all
changes into a single commit at the end. Run tests before each commit.

## Overview

NAS in VergeOS runs as a service VM hosting file-level storage on top of the vSAN. Currently "Manage NAS services, volumes, and shares." — doesn't explain the service→volume→share hierarchy.

## What VergeOS NAS is (source from docs)

A NAS service is a dedicated VM that provides file-level storage (NFS and CIFS/SMB shares) on top of VergeOS vSAN storage. The hierarchy is: NAS Service → Volumes (filesystems) → Shares (NFS exports or CIFS shares). Volumes support snapshots (copy-on-write, up to 1,000 per volume) and sync to remote sites.

## Source files to modify

| File | Current help |
|------|-------------|
| `commands/nas.py` | "Manage NAS services, volumes, and shares." |
| `commands/nas_service.py` | "Manage NAS services." |
| `commands/nas_volume.py` | "Manage NAS volumes." |
| `commands/nas_volume_snapshot.py` | "Manage NAS volume snapshots." |
| `commands/nas_cifs.py` | "Manage CIFS/SMB shares." |
| `commands/nas_nfs.py` | "Manage NFS exports." |
| `commands/nas_user.py` | "Manage NAS users." |
| `commands/nas_sync.py` | "Manage NAS volume syncs." |
| `commands/nas_files.py` | "Browse NAS volume files." |

## Examples to include

```
# List NAS services
vrg nas service list

# List volumes on a NAS service
vrg nas volume list my-nas

# Create a volume
vrg nas volume create my-nas --name shared-data --size 100GB --tier 2

# Manage shares
vrg nas cifs list my-nas
vrg nas nfs list my-nas
vrg nas cifs create my-nas --volume shared-data --name smb-share

# Volume snapshots
vrg nas volume-snapshot list my-nas --volume shared-data
vrg nas volume-snapshot create my-nas --volume shared-data --name nightly

# Browse files
vrg nas files list my-nas --volume shared-data --path /
```

### Behavioral notes

```
Notes:
  NAS services are VMs — they must be running to serve shares. Use
  "vrg nas service start/stop" to control the service VM.

  The resource hierarchy is: NAS Service → Volumes → Shares.
  Most NAS subcommands require the NAS service name as the first argument.
```

## Documentation references

- Public docs NAS overview: `/Volumes/HOME/projects/docs/docs/product-guide/nas/overview.md`
- Public docs NAS service: `/Volumes/HOME/projects/docs/docs/product-guide/nas/nas-service.md`
- Public docs NAS volumes: `/Volumes/HOME/projects/docs/docs/product-guide/nas/nas-local-volumes.md`
- Public docs NAS shares: `/Volumes/HOME/projects/docs/docs/product-guide/nas/nas-shares.md`
- Public docs volume snapshots: `/Volumes/HOME/projects/docs/docs/product-guide/nas/volume-snapshots-restores.md`
- Wiki NAS service VM: `/Volumes/HOME/verge-vault/VergeOS/wiki/entities/nas-service-vm.md`
- Wiki NAS shares: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/nas-shares.md`
- Wiki volumes: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/volumes-management.md`

## Checklist

- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_service.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_volume.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_volume_snapshot.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_cifs.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_nfs.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_files.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_sync.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `nas_user.py`
- [ ] Add examples to leaf command docstrings (Typer reads these as help text for `@app.command()` functions)
- [ ] Test all help output renders correctly
- [ ] Commit changes
