# Help Text Improvement: `vrg task`

**Date:** 2026-04-16
**Status:** Draft
**Scope:** Task command group and subcommands (task, schedule, trigger, event, script)
**Depends on:** help-00-root (must be done first to enable `rich_markup_mode="markdown"` on the main app)

---

## Conventions (apply to all help text in this plan)

### Where help text lives

- **Group apps** (`typer.Typer()`): Help goes in the `help=` parameter. The callback docstring is ignored when `help=` is set — keep callback docstrings to one line (e.g., `"""Manage virtual machines."""`).
- **Leaf commands** (`@app.command()`): Help comes from the function docstring. This is the only place Typer reads it.
- Every `typer.Typer()` must have `rich_markup_mode="markdown"` so markdown renders in terminal output.

### Section order for group `help=` strings

1. **Description** (1-3 sentences): What this resource is in VergeOS and why it matters. Not just "Manage X."
2. **Agent guidance** (1-2 sentences): Which flags produce machine-readable output, what fields are useful to `--query`, any gotchas.
3. **Examples** (3-6 invocations): Real commands showing common workflows. Use `---` markdown separator before and after.
4. **Notes** (optional): Behavioral quirks, ordering of operations, required preconditions.

### Section order for leaf command docstrings

1. **One-line summary** (what the command does).
2. **1-3 examples** showing invocation with typical arguments.
3. **Behavioral note** (optional): e.g., "This is a destructive operation" or "Requires the VM to be stopped."

### Quality bar

- Examples must use realistic resource names (e.g., `web-01`, `internal-net`, `tier-2`), not placeholders like `<name>`.
- Every group must mention `-o json` for structured output somewhere.
- Exit code 7 (multiple matches) should be mentioned where name resolution applies.
- Match the voice of showboat/tracecc: plain English, no filler, designed for agents to parse.

### Commit discipline

Make atomic commits as you go — one commit per logical unit of work (e.g., one
subgroup file complete, or all leaf docstrings in a file). Do not batch all
changes into a single commit at the end. Run tests before each commit.

## Overview

The task engine is VergeOS's built-in automation system. Currently "Manage tasks." — doesn't explain that tasks are scheduled or triggered operations that run against resources.

## What tasks are (source from docs)

Tasks are automated operations in VergeOS — actions that can be scheduled, triggered by events, or run manually. The task engine supports schedules (cron-like recurring tasks), triggers (event-driven execution), and scripts (GCS automation). Common task types include snapshot creation, sync operations, and maintenance operations.

## Source files to modify

| File | Current help |
|------|-------------|
| `commands/task.py` | "Manage tasks." |
| `commands/task_schedule.py` | "Manage task schedules." |
| `commands/task_trigger.py` | "Manage task triggers." |
| `commands/task_event.py` | "Manage task events." |
| `commands/task_script.py` | "Manage task scripts." |

## Examples to include

```
# List recent tasks
vrg task list

# Get task details
vrg -o json task get 1234

# Task schedules
vrg task schedule list
vrg task schedule get nightly-snapshot

# Task events (history of triggered executions)
vrg task event list
```

### Behavioral notes

```
Notes:
  Tasks are operations that run against VergeOS resources. They can be
  one-time, scheduled (recurring), or event-triggered.

  Use "vrg task list" to see recent task executions and their status.
  Use "vrg task schedule list" to see configured recurring tasks.
```

## Documentation references

- Wiki task engine entity: `/Volumes/HOME/verge-vault/VergeOS/wiki/entities/task-engine.md`
- Wiki task & event system: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/task-and-event-system.md`
- Public docs task engine: `/Volumes/HOME/projects/docs/docs/product-guide/automation/task-engine.md`
- Public docs schedules: `/Volumes/HOME/projects/docs/docs/product-guide/automation/schedules.md`
- Public docs create tasks: `/Volumes/HOME/projects/docs/docs/product-guide/automation/create-tasks.md`
- Public docs scripts: `/Volumes/HOME/projects/docs/docs/product-guide/automation/scripts.md`

## Checklist

- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `task.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `task_event.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `task_schedule.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `task_script.py`
- [ ] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `task_trigger.py`
- [ ] Add examples to leaf command docstrings (Typer reads these as help text for `@app.command()` functions)
- [ ] Test all help output renders correctly
- [ ] Commit changes
