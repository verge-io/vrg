# Help Text Improvement: Remaining Command Groups

**Date:** 2026-04-16
**Status:** Draft
**Scope:** billing, completion, configure, file, gpu, shared-object, webhook
**Depends on:** help-00-root (must be done first to enable `rich_markup_mode="markdown"` on the main app)

---

## Conventions (apply to all help text in this plan)

### Where help text lives

- **Group apps** (`typer.Typer()`): Help goes in the `help=` parameter. The callback docstring is ignored when `help=` is set — keep callback docstrings to one line (e.g., `"""Manage virtual machines."""`).
- **Leaf commands** (`@app.command()`): Help comes from the function docstring. This is the only place Typer reads it.
- Every `typer.Typer()` must have `rich_markup_mode="markdown"` so markdown renders in terminal output.

### Section order for group `help=` strings

1. **Description** (1-3 sentences): What this resource is in VergeOS and why it matters. Not just "Manage X."
2. **Agent guidance** (1-2 sentences): Which flags produce machine-readable output, what fields are useful to `--query`, any gotchas.
3. **Examples** (3-6 invocations): Real commands showing common workflows. Use `---` markdown separator before and after.
4. **Notes** (optional): Behavioral quirks, ordering of operations, required preconditions.

### Section order for leaf command docstrings

1. **One-line summary** (what the command does).
2. **1-3 examples** showing invocation with typical arguments.
3. **Behavioral note** (optional): e.g., "This is a destructive operation" or "Requires the VM to be stopped."

### Quality bar

- Examples must use realistic resource names (e.g., `web-01`, `internal-net`, `tier-2`), not placeholders like `<name>`.
- Every group must mention `-o json` for structured output somewhere.
- Exit code 7 (multiple matches) should be mentioned where name resolution applies.
- Match the voice of showboat/tracecc: plain English, no filler, designed for agents to parse.

### Commit discipline

Make atomic commits as you go — one commit per logical unit of work (e.g., one
subgroup file complete, or all leaf docstrings in a file). Do not batch all
changes into a single commit at the end. Run tests before each commit.

## Overview

Smaller command groups that still need agent-friendly help text.

## Groups and descriptions

### `vrg billing`
Per-tenant resource usage reports. Used by service providers to track and bill tenant consumption.

### `vrg completion`
Generate shell completion scripts for bash, zsh, fish, PowerShell. Utility command — minimal help improvement needed.

### `vrg configure`
Manage CLI configuration profiles in `~/.vrg/config.toml`. Add/edit/remove profiles with host, credentials, and default settings. This is the setup command — an agent should be directed here first.

### `vrg file`
Manage media catalog files stored in the vSAN — ISOs, disk images, OVA/OVF imports. These are the installation media and disk images used to create VMs.

### `vrg gpu`
Manage GPU hardware and vGPU profiles. VergeOS supports GPU passthrough (SR-IOV, vfio) and virtual GPU profiles for AI/ML workloads.

### `vrg shared-object`
Manage objects shared between a host and its tenants (or between tenants). Shared objects allow resource sharing across tenant boundaries.

### `vrg webhook`
Manage webhook integrations for event-driven notifications to external systems.

## Source files to modify

| File | Current help |
|------|-------------|
| `commands/billing.py` | "Billing and usage reports." |
| `commands/completion.py` | "Generate shell completion scripts." |
| `commands/configure.py` | "Configure Verge CLI settings." |
| `commands/file.py` | "Manage media catalog files (ISO, disk images, OVA/OVF)." |
| `commands/gpu.py` | "Manage GPUs and vGPU profiles." |
| `commands/shared_object.py` | "Manage shared objects between tenants." |
| `commands/webhook.py` | "Manage webhook integrations." |

## Examples to include

```
# Configure a profile
vrg configure add-profile prod --host https://vergeos.example.com --token $TOKEN
vrg configure list-profiles

# Media files
vrg file list
vrg file upload ubuntu-22.04.iso

# GPU management
vrg gpu list

# Billing reports
vrg billing list --tenant acme-corp

# Webhooks
vrg webhook list
```

## Documentation references

- Wiki files subsystem: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/files-subsystem.md`
- Wiki GPU passthrough: `/Volumes/HOME/verge-vault/VergeOS/wiki/concepts/gpu-passthrough.md`
- Wiki shared objects entity: `/Volumes/HOME/verge-vault/VergeOS/wiki/entities/shared-objects.md`
- Public docs webhooks: `/Volumes/HOME/projects/docs/docs/product-guide/automation/webhooks.md`
- Public docs AI/GPU: `/Volumes/HOME/projects/docs/docs/product-guide/private-ai/`

## Checklist

- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `billing.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `completion.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `configure.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `file.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `gpu.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `shared_object.py`
- [x] Update `help=` and add `rich_markup_mode="markdown"` on `typer.Typer()` in `webhook.py`
- [x] Add examples to leaf command docstrings where meaningful (Typer reads these as help text for `@app.command()` functions)
- [x] Test all help output renders correctly
- [x] Commit changes
